两个角度
1. 自上而下
2. 自下而上

继承的格式
```java
class A{

}

class B extends A{

}
```
 类A：父类、superClass、超类、基类
 类B：子类、subClass、派生类

继承后子类获得父类所有的属性和方法
但是由于封装性的影响，可能子类不能调用父类中声明的属性或方法

默认的父类
Java中声明的类，如果没有显式的声明其父类时，则默认基础与java.lang.Object

Java支持多层继承
	直接父类、间接父类
但不支持多重继承
	一个子类不能有多个父类